# Database Configuration (use 'db' hostname in Codespaces/Docker)
DATABASE_URL=postgresql://postgres:postgres@db:5432/nodeapi?schema=public

# Server Configuration
PORT=3000

# File Upload
UPLOAD_DIR=./uploads/items

# PostgreSQL Credentials (optional - for docker-compose)
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=nodeapi
PGPORT=5432